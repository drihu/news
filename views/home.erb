<main class="main">
  <div class="container">
    <% if articles.empty? %>
      <div class="warning">There are no more articles</div>
    <% end %>
    <% articles.each do |article| %>
      <%= erb(:_article, locals: { article: article }) %>
    <% end %>
  </div>
</main>
